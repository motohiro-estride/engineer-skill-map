# v1 データモデル仕様

engineer-skill-map v1 の論理データモデル。元データ (`private/` 配下の Excel 2ファイル) を統合し、公開Web / Excel出力 / ローカル(自分用) の3コンテキストで参照される。

## 設計の前提

- **公開Web**: GitHub Pages で `/` に統合版を1サイト公開
- **Excel出力**: 同一データから生成、営業/面談者へファイル添付で渡す
- **ローカル(private)ビルド**: 自分用の閲覧、面談時の手元参照、メモ機能を含む
- v1 では Profile（ターゲット別シート分割）機能は持たない。**v2 で追加予定** （詳細は末尾「将来拡張枠」）

---

## 表示レベル (3段階)

各フィールドは下記いずれかのレベルに属する:

| レベル | 公開Web | Excel | ローカル | 例 |
| --- | --- | --- | --- | --- |
| **全公開** | ○ | ○ | ○ | 自己PR、技術スタック、プロジェクト概要 |
| **Excel-only** | ✕ | ○ | ○ | 氏名、最寄駅、勤務開始可能日、生年月日 |
| **自分のみ** | ✕ | ✕ | ○ | private_memo (Project / TechTag) |

実装方針: `private/` 配下のデータを公開Webビルドでは読み込まないことで、情報漏洩リスクをゼロにする。

---

## エンティティ一覧

| エンティティ | 件数 | 役割 |
| --- | --- | --- |
| Engineer | 1 | プロフィール (シングルトン) |
| Project | 約32件 | 参画プロジェクト |
| TechTag | 多数 | 技術タグ。評価レベルと自動算出フィールドを持つ |
| IndustryTag | 数〜十数件 | 業界タグ。移行時に手動付与 |
| Role | 数件 | ポジションのマスタ |
| Phase | 6件 (固定 enum) | 担当工程。マスタ不要、定数 |

---

## Engineer (シングルトン)

| フィールド | 型 | 表示レベル | メモ |
| --- | --- | --- | --- |
| 氏名 | string | 全公開 | **ぼかし表記** (イニシャル等、例: `MM`) で記載。実名は yaml に書かない (個人特定リスク回避) |
| 性別 | enum {男性, 女性} | 全公開 | |
| 生年月日 | date | Excel-only | (任意) 元 Excel に無いので移行直後は未設定。年齢の算出元 |
| 年齢 | int (derived) | 全公開 | 生年月日があれば自動算出 |
| 国籍 | string | 全公開 | |
| 最寄駅 | object{路線, 駅, 徒歩・バス分} | Excel-only | (任意) 元 Excel から取れない場合は未設定 |
| 最終学歴 | string | 全公開 | **ぼかし表記**で記載 (例: `関東私文大学卒`)。実名校名は yaml に書かない (個人特定リスク回避) |
| 勤務開始可能日 | date | Excel-only | (任意) 鮮度劣化が早い、移行時に取れなければ未設定 |
| 勤務可能地域 | string | 全公開 | |
| 残業可否 | object{可否, 上限h} | 全公開 | |
| 休日出勤可否 | enum | 全公開 | |
| 出張可否 | enum | 全公開 | |
| 業務知識 | string | 全公開 | |
| 自己PR | text | 全公開 | |
| 経験年数 | float (derived) | 全公開 | 全 Project (**archived 含む**) の MIN(start) 〜 MAX(end) レンジから自動算出。「業界経験」を表すため OJT 等 archived も含める |
| 資格[] | sub-entity | 全公開 | 下記 Qualification |

### Qualification (Engineer に N持ち)

| フィールド | 型 |
| --- | --- |
| 名称 | string |
| 取得年月 | year-month |

> **Note**: 当初は `LanguageSkill` (TOEIC/英検 + 4軸スキル) を持たせる想定だったが、2026-04-25 時点で **LanguageSkill 全体を廃止** に変更。将来必要になれば Engineer の sub-entity として再導入可。

---

## Project (約32件)

| フィールド | 型 | 表示レベル | メモ |
| --- | --- | --- | --- |
| 通し番号 | int | 全公開 | 自動採番 (時系列降順) |
| プロジェクト名 | string | 全公開 | "某〇〇" or 実名 (実名は契約上承諾済の場合のみ) |
| 開始年月 | year-month | 全公開 | |
| 終了年月 | year-month \| null | 全公開 | 進行中は null |
| 期間 | string (derived) | 全公開 | "X年Yヶ月" 自動算出 |
| 概要 | text | 全公開 | |
| 役割 | Role 参照 | 全公開 | マスタ |
| 全体規模 | int | 全公開 | (任意) プロジェクト全体人数。MM のみ案件は移行時に取得不可、後で補完 |
| チーム規模 | int | 全公開 | (任意) 自分のチーム人数 |
| 担当工程 | bool×6 | 全公開 | 要件定義/基本設計/詳細設計/製造/テスト/保守 |
| 技術タグ[] | TechTag 参照 | 全公開 | M:N |
| 業界タグ[] | IndustryTag 参照 | 全公開 | M:N、移行時に手動付与 |
| 並行フラグ | bool | 全公開 | 他プロジェクトと並行参画した期間か |
| ビジネス上の目的 | text (任意) | 全公開 | スキルシート②由来。現状2件のみ記載あり |
| システム概要 | text (任意) | 全公開 | 同上 |
| 役割・貢献点 | text (任意) | 全公開 | 同上 |
| private_memo | text | 自分のみ | 「面談で聞かれたら話すこと」 |
| **visibility** | enum | (制御フィールド) | 下記参照 |

### visibility ステータス (3値)

| visibility | Web | Excel | 集計に含む | 詳細ページ | ローカル |
| --- | --- | --- | --- | --- | --- |
| **default** (デフォルト) | ○ | ○ | ✓ | ○ | ○ |
| **stats_only** | ✕ | ✕ | ✓ | ✕ (404) | ○ |
| **archived** | ✕ | ✕ | ✕ | ✕ (404) | ○ |

- **default**: 通常案件
- **stats_only**: 表に出したくないが経験としてカウントしたい案件 (例: NDA 厳しめだが技術的経験は計上したい)
- **archived**: 完全に外したい案件 (例: 新人OJT、技術的価値の薄いもの)

ローカル(private)ビルドでは、status に関わらず全件表示。自分の棚卸し用途のため。

---

## TechTag

| フィールド | 型 | 表示レベル | メモ |
| --- | --- | --- | --- |
| 名前 | string | 全公開 | 表示用。バージョンも別タグとして登録 (例: `React`, `React 18`, `React 17`) |
| baseName | string | 全公開 | フィルタ集約用 (例: `React 18` の baseName は `React`) |
| カテゴリ | enum | 全公開 | Language / Framework / OS / Middleware / Database / Tool / Cloud |
| 累積経験年数 | float (derived) | 全公開 | 関連 Project の期間合計。`archived` 除外、`stats_only` 含む |
| 関連プロジェクト件数 | int (derived) | 全公開 | 同集計ルール |
| private_memo | text | 自分のみ | 「Reactと聞かれたら〜」のような切り口別カンペ |

> **Note**: 当初は MM の保有技術一覧から取った `evaluationLevel` (A〜E) を持たせる想定だったが、2026-04-25 時点で **評価レベルは概念ごと廃止**。代わりに `累積経験年数` と `関連プロジェクト件数` を客観指標として用いる。将来再導入する場合は `TechTag.public.evaluationLevel` として復活可。

---

## IndustryTag

| フィールド | 型 |
| --- | --- |
| 名前 | string |
| (任意) 説明 | text |

例: 公共, 通信, 金融, NFT/Web3, 不動産, コーポレートサイト, 教育, エンタメ, 受託SES など。
ユーザが移行時に手動でタグ付け。

---

## Role

| フィールド | 型 |
| --- | --- |
| 名前 | string |
| 序列 | int | 表示順用 (PM > PL > サブリーダー > SE > PG など) |

---

## Phase (定数 enum)

`["要件定義", "基本設計", "詳細設計", "製造", "テスト", "保守"]` の6種固定。マスタテーブルは不要。

---

## 派生フィールドの算出ルール

| 派生フィールド | 算出元 | ルール |
| --- | --- | --- |
| Engineer.年齢 | Engineer.生年月日 | 現在日付との差 |
| Engineer.経験年数 | 全 Project の期間 (**archived 含む**) | MIN(start) 〜 MAX(end) のレンジ。archived (新人OJT 等) も「業界に居る期間」として含める。stats とは別系統 |
| Project.期間 | 開始年月, 終了年月 | "X年Yヶ月" |
| TechTag.累積経験年数 | 関連 Project の期間 | 合算。`archived` 除外、`stats_only` 含む |
| TechTag.関連プロジェクト件数 | 関連 Project | COUNT。同集計ルール |

---

## 元データとの対応

| エンティティ | 主な元データ |
| --- | --- |
| Engineer | MM冒頭 (技術経歴書ヘッダ) + スキルシート冒頭 (キャリアシートヘッダ) |
| Project | MM (30件) + スキルシート (18件、2024年以降の最新分はこちらのみ) を統合 |
| TechTag | MM の保有技術一覧 + 全 Project の使用技術 |
| IndustryTag | 移行時に Project から手動派生 |
| Role | MM/スキルシート の "役割/ポジション" 列から正規化 |
| Phase | MM の担当工程●フラグ列 |

---

## 将来拡張枠 (v2 以降の検討材料)

v1 のスキーマを非破壊で拡張可能な形で残しておくべきもの:

### 1. Output Profile (シートのバリエーション)

ターゲット (案件種別/業界/読者) に応じた **複数バリエーションのスキルシート** を出し分ける機能。

- 新エンティティ `OutputProfile` を導入 (id, name, description)
- `Project` に `profiles: OutputProfile[]` フィールドを追加 (デフォルト空配列 = 全 Profile に表示、非破壊変更)
- 公開Web は Profile ごとに別 URL を生成、Excel はビルド時に Profile 指定
- v1 ではこの機能を持たないが、データモデル設計はこの拡張を想定して妥協していない

#### v2 で要再議論となる派生事項

- **集計ポリシー**: Profile 単位の集計か全件横断か (v1 の方針は「全件横断」= 案 a)
- **配布運用**: GitHub Pages は認証なしのため、URL 体系をどう設計するか
  - A. 運用で割り切る / B. 推測困難な suffix / C. JSパスワードゲート / D. Cloudflare Pages + Access に乗り換え / E. Web は default のみ、特化版は Excel/PDF のみで配布
  - v1 では default 1本のみのため問題は発生しない
- **Profile の管理粒度**: v2 で実装するときに「最初に何個 Profile を作るか」も決める

### 2. 案件管理機能

営業から紹介された案件、面談スケジュール、結果トラッキングを扱う「自分専用」機能。スキルシートとは別レイヤだが、データソース (技術タグ・業界タグ) は共通利用したい。

- v1 は GitHub Pages 静的サイト前提で対応不可
- v2 で改めて、A. localStorage / B. GitHub をDB / C. Pages を捨ててバックエンド導入 / D. 外部SaaS 連携 のいずれかを選択

---

## v1 で実装しないと明記しておくべきこと (No-Goals)

- 認証機能
- 動的なデータ書き込み (UI からの編集)
- 案件管理機能
- Profile 機能 (ターゲット別シート分割)
- 多言語対応 (日本語のみ)
